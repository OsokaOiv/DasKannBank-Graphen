# DKB Finanz-Dashboard — Web version (backup)

Browser-only version using FastAPI + React. Requires Python backend.

```bash
# Terminal 1: Start API
.venv/bin/python api.py

# Terminal 2: Start frontend
cd web-dashboard && npm install && npm run dev
```

This is a transitional backup. The primary version is in `desktop/`.
